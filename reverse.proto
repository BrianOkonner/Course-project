syntax = "proto3";

package proto;

option go_package = "github.com/BrianOkonner/Course-project";

service Reverse {
    rpc Do(Request) returns (Response);
}

message Request {
    string message = 1;
}

message Response {
    string message = 1;
}
